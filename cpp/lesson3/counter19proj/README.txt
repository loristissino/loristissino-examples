Per capire come funziona il polimorfismo, proviamo a creare un
array di (puntatori a) contatori, e di istanziarne alcuni di un
tipo e alcuni di un altro.

Vedremo anche a cosa serve la parola chiave virtual e che conseguenze
si hanno nell'uso degli oggetti istanziati.

Modificheremo inoltre il Makefile usando dei segnaposto generici,
visto che da ogni file .cpp dobbiamo ottenere un file .o.

